const Header = () => {
  const template = `
    <header class="header">
      <img src="./img/logo.png" alt="logo" tabindex="0" />
      <h1>Platform Admin</h1>
    </header>
    `;

  return template;
};

export default Header;
